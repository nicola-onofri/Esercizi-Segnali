function xp = periodicize(x,T,t,dt)
    %PERIODICIZE periodicizzazione del segnale
    % x = il segnale da periodicizzare 
    % T = il passo con cui si vuole periodicizzare il segnale
    % t = il vettore dei tempi
    % dt = il passo del vettore dei tempi
    
    xp=zeros(size(t));
    %TODO
%     for n = floor(min(t)/T):ceil(max(t)/T)
%         xT = xT+myshift(xp,n*T/dt);
%     end

end

